# Backend configuration for the remote backend infrastructure
# This uses local state since we're creating the remote backend resources

terraform {
  # Using local state for the backend infrastructure itself
  # This is the recommended approach for creating remote backend resources
} 