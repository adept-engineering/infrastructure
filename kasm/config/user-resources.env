# Per-user RAM tiers (MiB). First match wins; * is the default for everyone else.
# Apply with: ./scripts/apply-user-resources.sh
#
# username=mib   (Kasm login username, not kasm-user)
remikuti=16384
*=2048
